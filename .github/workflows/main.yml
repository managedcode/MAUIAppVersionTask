name: main

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:


jobs:
   build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2.5.2
      
      - name: npm install
        run: npm install -g azure-pipelines-task-lib
      - name: npm install
        run: npm install -g mocha
      - name: npm install
        run: npm install -g sync-request
      - name: npm install
        run: npm install -g @types/q
      - name: npm install
        run: npm install -g @types/node
        
      - name: Test something
        run: echo "Test CI/CD without command"
        
      - name: Test Mocha
        run: mocha tasks/tests/_suite.js

